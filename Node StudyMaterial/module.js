//using cal module 
let calculator=require('./cal.js'); // here ./mean the module is in the same pack
let res=calculator.add(9,1);
let minus=calculator.sub(1,1);
console.log(minus);
console.log(res);